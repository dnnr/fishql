# Source this in per-session config
# can set:
# - fishql_dbprog : The Sqlite3 program, defaults to "sqlite3"
# - fishql_db : The DB "name" (not file) for the session, you may change it any time.

fishql init

